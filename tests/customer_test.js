'use strict';

var mocha = require('mocha');
var expect = require('chai').expect;

describe('Customer', function() {
  describe('ValidCustomer', function() {
    it('should pass because Customer has no functionality', function(done) {
      expect(true).ok;
    })
  });
});